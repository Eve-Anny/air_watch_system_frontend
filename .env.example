# Copy to .env.local (gitignored) for local dev, or set as a build-time env var on your hosting
# platform (e.g. Vercel) for the deployed build. No trailing slash.

VITE_API_BASE_URL=http://localhost:8000
